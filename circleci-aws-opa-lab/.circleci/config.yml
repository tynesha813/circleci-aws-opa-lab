version: 2.1

jobs:
  test:
    docker:
      - image: cimg/base:stable
    steps:
      - checkout
      - run:
          name: Hello World
          command: echo "Tynesha is working in CircleCI!"

workflows:
  test-workflow:
    jobs:
      - test